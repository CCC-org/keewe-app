import { Text, Card } from 'react-native-paper';
import React from 'react';

const ActivitySelectionScreen = () => {
  return (
    <Card>
      <Text>ActivitySelectionScreen</Text>
    </Card>
  );
};

export default ActivitySelectionScreen;
